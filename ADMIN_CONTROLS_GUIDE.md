# دليل صلاحيات الأدمن وميزة رفع اليد

## نظرة عامة

تم إضافة صلاحيات تحكم للأدمن (المضيف والمساعد) وميزة رفع اليد للمستخدمين العاديين في نظام الغرف.

---

## 🎯 الميزات المضافة

### 1. صلاحيات الأدمن (HOST & COHOST)

#### **كتم المايك للمستخدمين**
- يستطيع الأدمن كتم مايك أي مستخدم في الغرفة
- يظهر زر كتم المايك (🔇) على فيديو كل مستخدم
- عند الضغط، يتم كتم المايك تلقائياً للمستخدم المستهدف

#### **إغلاق الكاميرا للمستخدمين**
- يستطيع الأدمن إغلاق كاميرا أي مستخدم في الغرفة
- يظهر زر إغلاق الكاميرا (📷) على فيديو كل مستخدم
- عند الضغط، يتم إغلاق الكاميرا تلقائياً للمستخدم المستهدف

---

### 2. ميزة رفع اليد (للمستخدمين العاديين)

#### **كيفية الاستخدام**
- يظهر زر رفع اليد (✋) في شريط التحكم السفلي
- عند الضغط، يتم رفع اليد ويظهر مؤشر أصفر على الفيديو
- يمكن إنزال اليد بالضغط على الزر مرة أخرى
- جميع المشاركين (بما فيهم الأدمن) يرون المؤشر

---

## 🔧 التفاصيل التقنية

### الملفات المعدلة

1. **`apps/web/src/components/custom-video-conference.tsx`** (جديد)
   - مكون مخصص لعرض الفيديو مع الصلاحيات
   - يحتوي على منطق التحكم بالأدمن
   - يدير حالة رفع اليد

2. **`apps/web/src/app/room/[roomId]/page.tsx`** (محدث)
   - استخدام `CustomVideoConference` بدلاً من `VideoConference`
   - تمرير `userRole` للمكون

---

## 📡 آلية العمل

### التواصل بين المشاركين

يتم استخدام **LiveKit Data Channel** لإرسال الأوامر:

#### 1. رفع اليد
```json
{
  "type": "hand-raise",
  "raised": true,
  "participantId": "user-id"
}
```

#### 2. كتم المايك (من الأدمن)
```json
{
  "type": "admin-mute",
  "targetId": "user-id"
}
```

#### 3. إغلاق الكاميرا (من الأدمن)
```json
{
  "type": "admin-disable-camera",
  "targetId": "user-id"
}
```

---

## 🎨 واجهة المستخدم

### عرض الفيديو

```
┌─────────────────────────────────┐
│  📹 فيديو المستخدم              │
│                                 │
│  ✋ (مؤشر رفع اليد - أصفر)      │
│                                 │
│  🔇 📷 (أزرار الأدمن - أحمر)    │
│  👤 اسم المستخدم               │
└─────────────────────────────────┘
```

### شريط التحكم

**للأدمن:**
```
🎤 📹 🖥️ 🚪
```

**للمستخدمين:**
```
🎤 📹 🖥️ ✋ 🚪
```

---

## 🔐 الأمان

- **التحقق من الصلاحيات**: يتم التحقق من `userRole` قبل عرض أزرار التحكم
- **الأدوار المسموحة للتحكم**: `HOST` و `COHOST` فقط
- **حماية من إساءة الاستخدام**: الأوامر تُنفذ فقط على المستخدم المستهدف

---

## 🧪 الاختبار

### سيناريو 1: كتم المايك
1. سجل دخول كأدمن (HOST)
2. انضم إلى غرفة
3. انتظر مستخدم آخر للانضمام
4. اضغط زر كتم المايك على فيديو المستخدم
5. تحقق من كتم المايك تلقائياً

### سيناريو 2: رفع اليد
1. سجل دخول كمستخدم عادي (PARTICIPANT)
2. انضم إلى غرفة
3. اضغط زر رفع اليد (✋)
4. تحقق من ظهور المؤشر الأصفر
5. تحقق من رؤية الأدمن للمؤشر

### سيناريو 3: إغلاق الكاميرا
1. سجل دخول كأدمن (HOST)
2. انضم إلى غرفة
3. انتظر مستخدم آخر للانضمام
4. اضغط زر إغلاق الكاميرا على فيديو المستخدم
5. تحقق من إغلاق الكاميرا تلقائياً

---

## 📝 ملاحظات

- **الأداء**: استخدام Data Channel أسرع من WebSocket للأوامر الفورية
- **التوافق**: يعمل مع جميع المتصفحات الحديثة
- **القابلية للتوسع**: يمكن إضافة أوامر جديدة بسهولة

---

## 🚀 التطوير المستقبلي

### ميزات مقترحة:
- [ ] إضافة صلاحية "طرد من الغرفة" للأدمن
- [ ] إضافة إشعارات صوتية عند رفع اليد
- [ ] إضافة سجل لأوامر الأدمن
- [ ] إضافة صلاحية "منع الكلام" المؤقت
- [ ] إضافة قائمة بالأيدي المرفوعة للأدمن

---

## 🐛 استكشاف الأخطاء

### المشكلة: أزرار الأدمن لا تظهر
**الحل**: تحقق من أن `userRole` هو `HOST` أو `COHOST`

### المشكلة: رفع اليد لا يعمل
**الحل**: تحقق من اتصال LiveKit Data Channel

### المشكلة: الأوامر لا تُنفذ
**الحل**: تحقق من console للأخطاء وتأكد من اتصال جميع المشاركين

---

## 📞 الدعم

للمزيد من المساعدة، راجع:
- [LiveKit Documentation](https://docs.livekit.io/)
- [React Components Guide](https://docs.livekit.io/guides/room/react/)
