# اختبار صلاحيات الأدمن - دليل سريع

## 🔧 التعديلات التي تمت:

1. ✅ تبسيط شرط عرض أزرار الأدمن
2. ✅ إضافة `z-index` و `shadow` لجعل الأزرار أكثر وضوحاً
3. ✅ إضافة تسجيل Debug للتشخيص

---

## 🧪 خطوات الاختبار:

### **الخطوة 1: أعد تشغيل Frontend**

```powershell
cd d:/live_app/apps/web
npm run dev
```

---

### **الخطوة 2: افتح Console في المتصفح**

- اضغط `F12` لفتح Developer Tools
- اذهب إلى تبويب **Console**

---

### **الخطوة 3: سجل دخول كأدمن**

1. افتح المتصفح على `http://localhost:3002`
2. سجل دخول كـ **المسؤول الرئيسي** (admin@example.com)
3. أنشئ غرفة جديدة أو انضم لغرفة موجودة

---

### **الخطوة 4: افتح نافذة ثانية (مستخدم عادي)**

1. افتح نافذة Incognito/Private جديدة
2. اذهب إلى `http://localhost:3002`
3. سجل دخول كمستخدم عادي
4. انضم لنفس الغرفة

---

### **الخطوة 5: تحقق من Console**

في نافذة الأدمن، ستجد رسائل مثل:

```
🔍 Admin Controls Debug: {
  userRole: "HOST",
  isAdmin: true,
  UserRoleHOST: "HOST",
  UserRoleCOHOST: "COHOST",
  tracksCount: 2,
  participantsCount: 2
}

👤 Participant: {
  name: "المسؤول الرئيسي",
  isLocal: true,
  isAdmin: true,
  shouldShowAdminControls: false,
  participantType: "LocalParticipant"
}

👤 Participant: {
  name: "مستخدم عادي",
  isLocal: false,
  isAdmin: true,
  shouldShowAdminControls: true,
  participantType: "RemoteParticipant"
}
```

---

## ✅ ما يجب أن تراه:

### **في نافذة الأدمن:**

- فيديو المستخدم العادي يجب أن يحتوي على:
  - زرين أحمرين في الزاوية السفلية اليمنى
  - 🔇 زر كتم المايك
  - 📷 زر إغلاق الكاميرا

### **في نافذة المستخدم العادي:**

- زر ✋ رفع اليد في شريط التحكم السفلي

---

## 🐛 إذا لم تظهر الأزرار:

### **تحقق من Console:**

1. **هل `isAdmin` = `true`؟**
   - إذا `false`، المشكلة في `userRole`

2. **هل `shouldShowAdminControls` = `true` للمستخدم الآخر؟**
   - إذا `false`، المشكلة في الشرط

3. **هل `participantType` = `"RemoteParticipant"`؟**
   - إذا لا، المشكلة في نوع المشارك

---

## 📸 الشكل المتوقع:

```
┌─────────────────────────────────┐
│  📹 فيديو المستخدم العادي       │
│                                 │
│                                 │
│                                 │
│  👤 اسم المستخدم    🔇 📷      │
└─────────────────────────────────┘
         ↑              ↑  ↑
    اسم المستخدم    أزرار الأدمن
```

---

## 📋 معلومات Debug المهمة:

أرسل لي من Console:

1. قيمة `isAdmin`
2. قيمة `shouldShowAdminControls` للمستخدم الآخر
3. قيمة `participantType` للمستخدم الآخر
4. أي أخطاء في Console

---

## 🔄 بعد التأكد من العمل:

سأقوم بإزالة رسائل Debug وتنظيف الكود.
