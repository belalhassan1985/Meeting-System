# تثبيت المكتبات المطلوبة للنظام الإداري

## Backend - مكتبات إضافية

يجب تثبيت المكتبات التالية في `apps/api`:

```powershell
cd d:\live_app\apps\api
npm install bcrypt jsonwebtoken
npm install --save-dev @types/bcrypt @types/jsonwebtoken
```

## Frontend - جاهز

جميع المكتبات المطلوبة للـ Frontend موجودة مسبقاً.

---

## إنشاء جدول admins وأول مسؤول

بعد تثبيت المكتبات، يجب إنشاء جدول `admins` في قاعدة البيانات:

### ⭐ الطريقة 1: استخدام Node.js Script (الأسهل والموصى بها)

```powershell
cd d:\live_app\apps\api
node scripts/create-admin.js
```

هذا سيقوم بـ:
- ✅ إنشاء جدول `admins` تلقائياً
- ✅ إضافة أول مسؤول (username: admin, password: admin123)
- ✅ تشفير كلمة المرور باستخدام bcrypt

---

### الطريقة 2: استخدام PowerShell Script

```powershell
cd d:\live_app
.\setup_admin.ps1
```

---

### الطريقة 3: يدوياً عبر SQL

افتح pgAdmin أو psql وقم بتنفيذ:

```powershell
psql -h localhost -p 5432 -U postgres -d arabic_meet -f create_admins_table.sql
```

أو انسخ محتوى ملف `create_admins_table.sql` ونفذه في pgAdmin.

---

### الطريقة 4: عبر API بعد إنشاء الجدول

إذا كان الجدول موجوداً، يمكنك إضافة مسؤول جديد:

```powershell
curl -X POST http://localhost:3001/auth/register `
  -H "Content-Type: application/json" `
  -d '{\"username\":\"admin\",\"password\":\"admin123\",\"fullName\":\"المسؤول الرئيسي\"}'
```

---

## تشغيل النظام الكامل

### 1. تشغيل PostgreSQL و Redis (Docker)
```powershell
cd d:\live_app
docker compose -f docker-compose-livekit-only.yml up -d
```

### 2. تشغيل LiveKit (Windows Binary)
```powershell
cd C:\livekit
.\livekit-server.exe --config livekit.yaml
```

### 3. تشغيل Backend
```powershell
cd d:\live_app\apps\api
npm run dev
```

### 4. تشغيل Frontend
```powershell
cd d:\live_app\apps\web
npm run dev
```

---

## الوصول للنظام

- **الصفحة الرئيسية**: http://localhost:3000
- **لوحة التحكم الإدارية**: http://localhost:3000/admin/login
- **API Backend**: http://localhost:3001

---

## الميزات المتوفرة

### ✅ للمستخدمين العاديين:
- إنشاء غرف اجتماعات
- الانضمام للغرف
- مكالمات فيديو وصوت
- مشاركة الشاشة

### ✅ للمسؤولين:
- تسجيل الدخول الآمن
- لوحة تحكم شاملة مع إحصائيات
- إدارة المستخدمين (عرض/حذف)
- إدارة الغرف (عرض/إغلاق/إعادة فتح/حذف)
- سجلات الأحداث مع تصفية
- إدارة المسؤولين (إضافة/تعديل/حذف)
- Sidebar عربي احترافي

---

## ملاحظات مهمة

1. **أمان كلمات المرور**: تأكد من استخدام كلمات مرور قوية للمسؤولين
2. **JWT Secret**: في الإنتاج، غيّر `JWT_SECRET` في `.env`
3. **HTTPS**: للوصول من الشبكة المحلية، راجع `HTTPS_SETUP.md` أو `USE_NGROK.md`

---

**النظام جاهز للاستخدام! 🎉**
