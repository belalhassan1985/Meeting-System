# 🔐 دليل نظام المصادقة للمستخدمين

## 📋 نظرة عامة

تم تحديث النظام ليتطلب **تسجيل دخول إلزامي** لجميع المستخدمين قبل الانضمام للاجتماعات. الآن فقط المستخدمون المسجلون في النظام يمكنهم الدخول للاجتماعات.

---

## 🎯 التغييرات الرئيسية

### 1. **جدول المستخدمين المحدّث**
تم إضافة حقول المصادقة:
- `username` - اسم المستخدم (فريد)
- `password` - كلمة المرور (مشفرة)
- `isActive` - حالة المستخدم (نشط/معطل)

### 2. **نظام تسجيل الدخول للمستخدمين**
- صفحة تسجيل دخول خاصة بالمستخدمين: `/user-login`
- التحقق من البيانات قبل السماح بالدخول
- إنشاء JWT token للمستخدم المصادق

### 3. **لوحة إدارة المستخدمين**
- المسؤول يمكنه إضافة/تعديل/حذف المستخدمين
- تفعيل/تعطيل حسابات المستخدمين
- إدارة كلمات المرور

---

## 🚀 كيفية البدء

### الخطوة 1: تحديث قاعدة البيانات

نفذ هذا الأمر لإضافة الحقول الجديدة:

```bash
cd apps/api
node scripts/update-users-table.js
```

### الخطوة 2: إعادة تشغيل Backend

```bash
cd apps/api
npm run dev
```

### الخطوة 3: إعادة تشغيل Frontend

```bash
cd apps/web
npm run dev
```

---

## 👥 إدارة المستخدمين (للمسؤول)

### إضافة مستخدم جديد:

1. سجل دخول كمسؤول: http://localhost:3000/login
2. اذهب إلى **لوحة التحكم** → **إدارة المستخدمين**
3. اضغط **إضافة مستخدم جديد**
4. املأ البيانات:
   - **اسم المستخدم**: username فريد
   - **كلمة المرور**: كلمة مرور قوية
   - **الاسم الكامل**: اسم المستخدم الحقيقي
   - **البريد الإلكتروني**: (اختياري)
5. اضغط **إنشاء**

### تعديل مستخدم:

1. في صفحة **إدارة المستخدمين**
2. اضغط على أيقونة **تعديل** بجانب المستخدم
3. عدّل البيانات المطلوبة
4. اضغط **حفظ التغييرات**

### تعطيل/تفعيل مستخدم:

1. في صفحة **إدارة المستخدمين**
2. اضغط على أيقونة **الطاقة** بجانب المستخدم
3. سيتم تبديل الحالة تلقائياً

### حذف مستخدم:

1. في صفحة **إدارة المستخدمين**
2. اضغط على أيقونة **سلة المهملات**
3. أكد الحذف

---

## 🔑 تسجيل الدخول (للمستخدمين)

### الطريقة الصحيحة:

1. افتح: http://localhost:3000
2. سيتم توجيهك تلقائياً لصفحة تسجيل الدخول
3. أدخل:
   - **اسم المستخدم**: الذي أعطاه لك المسؤول
   - **كلمة المرور**: الذي أعطاها لك المسؤول
4. اضغط **تسجيل الدخول**
5. سيتم توجيهك للوحة التحكم (Dashboard)
6. الآن يمكنك إنشاء أو الانضمام للاجتماعات

---

## 🔒 الأمان

### ما تم تأمينه:

✅ **لا يمكن الدخول للاجتماعات بدون تسجيل دخول**
- Dashboard محمي بالكامل
- يتم التحقق من Token في كل طلب
- المستخدمون المعطلون لا يمكنهم الدخول

✅ **كلمات المرور مشفرة**
- استخدام bcrypt للتشفير
- لا يتم تخزين كلمات المرور بشكل نصي

✅ **JWT Tokens**
- صلاحية 7 أيام
- يحتوي على معلومات المستخدم
- يتم التحقق منه في كل طلب

✅ **فصل الصلاحيات**
- المسؤولون لهم لوحة تحكم منفصلة
- المستخدمون لهم Dashboard منفصل
- لا يمكن للمستخدم الوصول للوحة المسؤول

---

## 📊 التدفق الكامل

### للمستخدم الجديد:

```
1. المسؤول يُنشئ حساب للمستخدم
   ↓
2. المستخدم يحصل على username + password
   ↓
3. المستخدم يفتح النظام
   ↓
4. يسجل دخول بالبيانات
   ↓
5. يدخل Dashboard
   ↓
6. يُنشئ أو ينضم لاجتماع
   ↓
7. يدخل الاجتماع بنجاح ✅
```

### للمستخدم غير المسجل:

```
1. شخص يحاول فتح النظام
   ↓
2. يتم توجيهه لصفحة تسجيل الدخول
   ↓
3. لا يملك username/password
   ↓
4. لا يمكنه الدخول ❌
   ↓
5. يجب عليه التواصل مع المسؤول
```

---

## 🔧 API Endpoints الجديدة

### تسجيل دخول المستخدم:
```
POST /users/login
Body: { username, password }
Response: { token, user }
```

### الحصول على جميع المستخدمين:
```
GET /users
Headers: { Authorization: Bearer <token> }
Response: [{ id, username, name, email, isActive, createdAt }]
```

### إنشاء مستخدم جديد:
```
POST /users
Headers: { Authorization: Bearer <admin_token> }
Body: { username, password, name, email? }
Response: { id, username, name, email }
```

### تحديث مستخدم:
```
PUT /users/:id
Headers: { Authorization: Bearer <admin_token> }
Body: { username?, password?, name?, email?, isActive? }
Response: { id, username, name, email, isActive }
```

### حذف مستخدم:
```
DELETE /users/:id
Headers: { Authorization: Bearer <admin_token> }
Response: { message }
```

### تبديل حالة المستخدم:
```
PUT /users/:id/toggle-status
Headers: { Authorization: Bearer <admin_token> }
Response: { id, username, name, isActive }
```

---

## 🎨 الصفحات الجديدة

### 1. `/user-login`
- صفحة تسجيل دخول المستخدمين
- تصميم مشابه لصفحة تسجيل دخول المسؤولين
- توجيه تلقائي للـ Dashboard بعد النجاح

### 2. `/admin/users-management`
- لوحة إدارة المستخدمين
- إضافة/تعديل/حذف المستخدمين
- تفعيل/تعطيل الحسابات
- بحث وفلترة

---

## ⚠️ ملاحظات مهمة

### للمسؤولين:

1. **أنشئ مستخدمين قبل الاستخدام**
   - لن يتمكن أي شخص من الدخول بدون حساب

2. **استخدم كلمات مرور قوية**
   - على الأقل 8 أحرف
   - مزيج من أحرف وأرقام

3. **راقب الحسابات النشطة**
   - عطّل الحسابات غير المستخدمة
   - احذف الحسابات القديمة

### للمستخدمين:

1. **احفظ بيانات الدخول**
   - لن تتمكن من الدخول بدونها

2. **لا تشارك كلمة المرور**
   - كل مستخدم له حساب خاص

3. **تواصل مع المسؤول**
   - إذا نسيت كلمة المرور
   - إذا واجهت مشاكل في الدخول

---

## 🐛 استكشاف الأخطاء

### المشكلة: "اسم المستخدم أو كلمة المرور غير صحيحة"
**الحل**: 
- تأكد من كتابة البيانات بشكل صحيح
- تحقق من أن الحساب نشط
- تواصل مع المسؤول

### المشكلة: لا يمكن إنشاء مستخدم جديد
**الحل**:
- تأكد من أن اسم المستخدم فريد
- تأكد من أن البريد الإلكتروني فريد (إن وجد)
- تحقق من الاتصال بقاعدة البيانات

### المشكلة: يتم توجيهي لصفحة تسجيل الدخول باستمرار
**الحل**:
- امسح cache المتصفح
- تأكد من أن Backend يعمل
- تحقق من صلاحية Token

---

## 📚 الملفات المعدّلة

### Backend:
- `apps/api/src/entities/user.entity.ts` - تحديث Entity
- `apps/api/src/services/user.service.ts` - خدمة المستخدمين (جديد)
- `apps/api/src/controllers/user.controller.ts` - Controller (جديد)
- `apps/api/src/app.module.ts` - تسجيل الخدمات
- `apps/api/scripts/update-users-table.js` - Script التحديث (جديد)

### Frontend:
- `apps/web/src/app/user-login/page.tsx` - صفحة تسجيل الدخول (جديد)
- `apps/web/src/app/admin/users-management/page.tsx` - إدارة المستخدمين (جديد)
- `apps/web/src/app/admin/layout.tsx` - إضافة رابط إدارة المستخدمين
- `apps/web/src/app/dashboard/page.tsx` - حماية Dashboard
- `apps/web/src/app/page.tsx` - توجيه ذكي

---

## ✅ الخلاصة

الآن النظام **آمن بالكامل**:
- ✅ فقط المستخدمون المسجلون يمكنهم الدخول
- ✅ المسؤول يتحكم في من يمكنه الوصول
- ✅ كلمات المرور مشفرة
- ✅ JWT Tokens للمصادقة
- ✅ فصل كامل بين المسؤولين والمستخدمين

**لم يعد بإمكان أي شخص خارجي الدخول للاجتماعات! 🎉**
