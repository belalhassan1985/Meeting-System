# تدفق النظام - System Flow

## 🔐 نظام تسجيل الدخول والصلاحيات

### تدفق المستخدم الأساسي:

```
1. المستخدم يفتح http://localhost:3000
   ↓
2. يتم التحقق من تسجيل الدخول
   ↓
   ├─ إذا لم يكن مسجل دخول → توجيه إلى /login
   └─ إذا كان مسجل دخول → توجيه إلى /dashboard
```

---

## 📄 الصفحات الرئيسية

### 1. **صفحة تسجيل الدخول** (`/login`)
- **الوصول**: للجميع (غير محمية)
- **الوظيفة**: تسجيل دخول المسؤولين والمستخدمين
- **بعد النجاح**: توجيه إلى `/dashboard`

### 2. **لوحة التحكم الرئيسية** (`/dashboard`)
- **الوصول**: للمستخدمين المسجلين فقط
- **الوظيفة**: 
  - إنشاء غرف اجتماعات جديدة
  - الانضمام لغرف موجودة
  - عرض قائمة الغرف النشطة
  - الوصول للوحة التحكم الإدارية (للمسؤولين)

### 3. **لوحة التحكم الإدارية** (`/admin`)
- **الوصول**: للمسؤولين المسجلين فقط
- **الصفحات الفرعية**:
  - `/admin` - الإحصائيات العامة
  - `/admin/users` - إدارة المستخدمين
  - `/admin/rooms` - إدارة الغرف
  - `/admin/audit-logs` - سجلات الأحداث
  - `/admin/admins` - إدارة المسؤولين

---

## 🔑 نظام المصادقة

### التخزين المحلي (LocalStorage):
- **`userToken`**: JWT token للمستخدم/المسؤول
- **`userInfo`**: معلومات المستخدم (id, username, fullName)

### التحقق من الصلاحيات:
```javascript
// في كل صفحة محمية
const token = localStorage.getItem('userToken')
if (!token) {
  router.push('/login')
}
```

---

## 🚀 خطوات الاستخدام

### للمستخدم العادي:
1. فتح `http://localhost:3000`
2. تسجيل الدخول باستخدام الحساب
3. إنشاء غرفة أو الانضمام لغرفة موجودة
4. بدء الاجتماع

### للمسؤول:
1. فتح `http://localhost:3000`
2. تسجيل الدخول باستخدام حساب المسؤول
   - Username: `admin`
   - Password: `admin123`
3. الوصول للوحة التحكم من الزر في الأعلى
4. إدارة النظام (مستخدمين، غرف، مسؤولين، سجلات)

---

## 📊 الصلاحيات

| الصفحة | مسجل دخول | غير مسجل |
|--------|----------|----------|
| `/` | ✅ توجيه لـ dashboard | ✅ توجيه لـ login |
| `/login` | ✅ متاح | ✅ متاح |
| `/dashboard` | ✅ متاح | ❌ توجيه لـ login |
| `/admin/*` | ✅ متاح | ❌ توجيه لـ login |
| `/room/[id]` | ✅ متاح | ⚠️ يحتاج token |

---

## 🔄 تسجيل الخروج

من أي صفحة، يمكن للمستخدم:
1. الضغط على زر "تسجيل الخروج"
2. يتم حذف `userToken` و `userInfo`
3. توجيه تلقائي إلى `/login`

---

## 🎯 الميزات الأمنية

✅ جميع كلمات المرور مشفرة باستخدام bcrypt  
✅ JWT tokens للجلسات  
✅ التحقق من الصلاحيات في كل صفحة  
✅ حماية API endpoints في Backend  
✅ فصل صلاحيات المستخدمين والمسؤولين  

---

## 📝 ملاحظات مهمة

1. **الحساب الافتراضي**: 
   - Username: `admin`
   - Password: `admin123`
   - يجب تغيير كلمة المرور بعد أول تسجيل دخول

2. **إضافة مستخدمين جدد**:
   - يتم من خلال لوحة التحكم الإدارية
   - `/admin/admins` → إضافة مسؤول جديد

3. **الأمان**:
   - تأكد من تغيير `JWT_SECRET` في الإنتاج
   - استخدم HTTPS في الإنتاج
   - غيّر كلمات المرور الافتراضية

---

**النظام الآن يعمل بشكل آمن ومنظم! 🎉**
