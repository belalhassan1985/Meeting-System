# 🚀 دليل التشغيل السريع

## ✅ النظام يعمل الآن!

### الخوادم النشطة:
- **Backend API**: `http://localhost:3001`
- **Frontend**: `http://localhost:3002` ⚠️ (تغير من 3000 إلى 3002)

---

## 🔓 تسجيل الدخول

### 1. افتح المتصفح:
```
http://localhost:3002
```

### 2. سيتم توجيهك تلقائياً لصفحة تسجيل الدخول

### 3. استخدم البيانات التالية:
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`

---

## 📱 بعد تسجيل الدخول

ستظهر لك صفحة Dashboard حيث يمكنك:
- ✅ إنشاء غرف اجتماعات جديدة
- ✅ الانضمام لغرف موجودة
- ✅ الوصول للوحة التحكم الإدارية (زر في الأعلى)

---

## 🛠️ لوحة التحكم الإدارية

من Dashboard، اضغط على زر "لوحة التحكم" للوصول إلى:
- إدارة المستخدمين
- إدارة الغرف (مع إمكانية إعادة الفتح)
- سجلات الأحداث
- إدارة المسؤولين

---

## ⚠️ ملاحظة مهمة

إذا كنت تريد استخدام المنفذ `3000` للـ Frontend:
1. أوقف أي خدمة تعمل على المنفذ `3000`
2. أعد تشغيل Frontend

```powershell
# إيقاف Frontend
Ctrl+C

# إعادة التشغيل
npm run dev
```

---

## 🔄 إعادة التشغيل الكاملة

إذا واجهت أي مشاكل:

### 1. أوقف جميع الخوادم (Ctrl+C)

### 2. شغل Backend:
```powershell
cd d:\live_app\apps\api
npm run dev
```

### 3. شغل Frontend (في terminal جديد):
```powershell
cd d:\live_app\apps\web
npm run dev
```

---

## ✨ النظام جاهز للاستخدام!

افتح `http://localhost:3002` وابدأ باستخدام النظام.
