# 🔍 دليل حل مشكلة تسجيل دخول المستخدمين

## ✅ تم التحقق من:

1. **قاعدة البيانات** ✅
   - المستخدمون موجودون
   - كلمات المرور مشفرة بشكل صحيح
   - جميع الحسابات نشطة

2. **Backend API** ✅
   - Endpoint `/users/login` يعمل بشكل صحيح
   - تم اختباره مباشرة ونجح
   - CORS مفعّل للـ localhost

---

## 🎯 الحل السريع:

### الخطوة 1: تأكد من تشغيل Backend
```bash
cd d:\live_app\apps\api
npm run dev
```
يجب أن ترى: `🚀 Arabic Meet API running on: http://localhost:3001`

### الخطوة 2: تأكد من تشغيل Frontend
```bash
cd d:\live_app\apps\web
npm run dev
```
يجب أن يعمل على: `http://localhost:3002` أو `http://localhost:3000`

### الخطوة 3: اختبر تسجيل الدخول
افتح: **http://localhost:3002/test-login**

اضغط زر "اختبار تسجيل الدخول" وانظر للنتيجة.

---

## 📝 بيانات الدخول الصحيحة:

| Username | Password | الاسم |
|----------|----------|-------|
| `testuser` | `123456` | مستخدم تجريبي |
| `علي` | `123456` | علي |
| `yyyyy` | `123456` | yyyyy |
| `بلالحسنعذاب` | `123456` | بلال حسن عذاب |

---

## 🔧 إذا استمرت المشكلة:

### 1. امسح Cache المتصفح
- اضغط `Ctrl + Shift + Delete`
- امسح Cookies و Cache
- أعد تحميل الصفحة

### 2. تحقق من Console المتصفح
- اضغط `F12`
- اذهب لتبويب **Console**
- ابحث عن أي أخطاء باللون الأحمر
- أرسل لي الأخطاء

### 3. تحقق من Network
- اضغط `F12`
- اذهب لتبويب **Network**
- حاول تسجيل الدخول
- ابحث عن طلب `users/login`
- انقر عليه وانظر:
  - **Request Headers**: هل `Content-Type: application/json`؟
  - **Request Payload**: هل البيانات صحيحة؟
  - **Response**: ما هو الرد؟

### 4. تحقق من API URL
افتح: `d:\live_app\apps\web\.env.local`

يجب أن يحتوي على:
```
NEXT_PUBLIC_API_URL=http://localhost:3001
```

إذا لم يكن موجوداً، أنشئه.

### 5. أعد تشغيل كل شيء
```bash
# أوقف Backend (Ctrl+C)
# أوقف Frontend (Ctrl+C)

# أعد تشغيل Backend
cd d:\live_app\apps\api
npm run dev

# في terminal جديد، أعد تشغيل Frontend
cd d:\live_app\apps\web
npm run dev
```

---

## 🧪 Scripts الاختبار:

### اختبار قاعدة البيانات:
```bash
cd d:\live_app\apps\api
node scripts/check-users.js
```

### اختبار تسجيل الدخول مباشرة:
```bash
cd d:\live_app\apps\api
node scripts/test-user-login.js
```

### اختبار API:
```bash
cd d:\live_app\apps\api
node scripts/test-api-login.js
```

### إعادة تعيين كلمة المرور:
```bash
cd d:\live_app\apps\api
node scripts/reset-user-password.js testuser 123456
```

---

## 🎯 الخطوات التالية:

1. **افتح صفحة الاختبار**: http://localhost:3002/test-login
2. **اضغط زر الاختبار**
3. **انظر للنتيجة**
4. **إذا نجح**: جرّب تسجيل الدخول العادي من http://localhost:3002/user-login
5. **إذا فشل**: افتح Console (F12) وأرسل لي الأخطاء

---

## ⚠️ أخطاء شائعة:

### خطأ: "Failed to fetch"
**السبب**: Backend لا يعمل
**الحل**: شغّل Backend على port 3001

### خطأ: "CORS error"
**السبب**: مشكلة في CORS
**الحل**: Backend يدعم CORS بالفعل، تأكد من إعادة تشغيله

### خطأ: "Invalid credentials"
**السبب**: Username أو Password خطأ
**الحل**: استخدم البيانات الصحيحة من الجدول أعلاه

### خطأ: "Network error"
**السبب**: API_URL خطأ
**الحل**: تحقق من ملف `.env.local`

---

## 📞 إذا احتجت مساعدة:

أرسل لي:
1. لقطة شاشة من صفحة `/test-login`
2. محتوى Console (F12 → Console)
3. محتوى Network (F12 → Network → users/login)

سأساعدك فوراً! 🚀
